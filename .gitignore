node_modules/
.DS_Store
*.log
.cache/
.external-*-link
.expo/
dist/
coverage/
.env
.env.*
!.env.example
supabase/.branches/
supabase/.temp/
.vercel/
.vercel
.env*

# The admin app is also published from its own repository, which cannot reach
# `../packages`. Its build vendors a copy into `admin/packages/`; here that copy
# is an artefact, and `packages/` is the source.
admin/packages/

# The Google Fonts checkout the importer reads: 2.8 GB of upstream repository,
# and not ours to carry. Clone it here, run
# `node supabase/scripts/import-google-fonts.mjs`, and the shelf is in Supabase
# where the app can reach it.
font-library/

# Head reconstruction: the master .blend, the GLB and the validation sheets are
# committed as deliverables. These two are large intermediates that
# scripts/mascot regenerates on the way to them.
output/likeness_fit.blend
output/likeness_features.blend
output/*.blend1

__pycache__/
*.pyc
